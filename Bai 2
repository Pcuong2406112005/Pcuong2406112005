#include<iostream>
#include<vector>
#include<set>
using namespace std;
int main() {
	int t;
	cin >> t;
	while (t--) {
		string a;
		cin >> a;
		if (a[0] == a[a.size() - 1]) cout << "YES";
		else cout << "NO";
		cout << endl;
	}
}
